{
  "name": "about.joe",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "verify:static": "npm run build && node scripts/verify-static-build.mjs",
    "analyze:assets": "node scripts/asset-baseline.mjs",
    "typecheck": "tsc --noEmit",
    "typecheck:manifest": "tsc --noEmit --allowJs --checkJs --target ES2022 --module ESNext --moduleResolution Bundler --skipLibCheck --lib ES2022,DOM src/global.d.ts src/types.ts scenes/manifest.js",
    "typecheck:runtime": "tsc --noEmit --allowJs --checkJs --target ES2022 --module ESNext --moduleResolution Bundler --skipLibCheck --lib ES2022,DOM src/global.d.ts src/types.ts shared/runtime.js"
  },
  "devDependencies": {
    "vite": "^7.1.3",
    "typescript": "^5.9.2"
  }
}
